use crate::arithmetic::basic_op;
/* 
pub fn gen_key_pair(&self, p: &u64, q: &u64) {
    // compute N
    let N = p * q;
    // compute λ
    let λ = basic_op::lcm(p-1, q-1);
    // random pick g between 1 and N^2 -1
    let g = &N;
    let L = &N;
        
}
    */